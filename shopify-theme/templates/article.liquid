<section class="iv-wrap iv-sec iv-prose">
  <h1>{{ article.title }}</h1>
  <p class="iv-note">{{ article.published_at | date: '%B %d, %Y' }}</p>
  <div style="margin-top:20px">{{ article.content }}</div>
</section>
